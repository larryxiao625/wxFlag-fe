<!--pages/user/shop/shop.wxml-->
<view class='container'>
  <block wx:for="{{goods}}">
    <button class='grid-nine'>
      <image class='icon' src='{{item.src}}'></image>
      <text>{{item.name}}</text>
    </button>
  </block>

</view>